# MemoryX 生产环境配置 (31.65)
# 复制到服务器: /etc/memoryx/api.env
# 权限: sudo chmod 600 /etc/memoryx/api.env

# ==========================================
# 数据库 (已有服务，不修改)
# ==========================================
DATABASE_URL=postgresql://memoryx:YOUR_DB_PASSWORD@localhost:5432/memoryx

# ==========================================
# Redis (已有服务，不修改)
# ==========================================
REDIS_URL=redis://localhost:6379/0

# ==========================================
# 安全密钥 (必须修改)
# 生成命令: openssl rand -hex 32
# ==========================================
SECRET_KEY=CHANGE_THIS_TO_RANDOM_32_BYTE_HEX_STRING

# ==========================================
# Ollama (已有服务，不修改)
# ==========================================
OLLAMA_HOST=http://192.168.31.65:11434

# ==========================================
# 应用配置
# ==========================================
APP_NAME=MemoryX Production
VERSION=1.0.0
DEBUG=false
ENVIRONMENT=production

# ==========================================
# 可选: 第三方服务
# ==========================================
# FIREBASE_CREDENTIALS_PATH=/etc/memoryx/firebase-credentials.json
# SENTRY_DSN=https://xxx@sentry.io/xxx
